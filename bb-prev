#!/bin/bash

# Get the path to me
SCRIPT_DIR=$( cd -- "$( dirname -- "${BASH_SOURCE[0]}" )" &> /dev/null && pwd )

source $SCRIPT_DIR/.env

# Send the "previous" command to mpv
echo '{ "command": ["playlist-prev"] }' | socat - "$SOCKET" > /dev/null
